# ============================================================
# Probabilistic Flight Delay Forecasting
# requirements.txt — pinned for reproducibility (Python 3.9+)
# ============================================================

# ── Core data manipulation ──────────────────────────────────
numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0          # Parquet I/O for BTS data files

# ── Scientific computing & statistics ──────────────────────
scipy==1.13.1
statsmodels==0.14.2      # ARIMA, SARIMA, SARIMAX, ADF, ACF/PACF, Granger
pmdarima==2.0.4          # auto_arima for model order selection
scikit-learn==1.5.0      # MinMaxScaler, train_test_split, metrics

# ── Deep learning ───────────────────────────────────────────
torch==2.3.1             # LSTM, GRU, QRNN (Pinball Loss)
# Note: Install CPU-only wheel if no CUDA GPU:
#   pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu

# ── Visualization ───────────────────────────────────────────
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0           # Interactive charts in dashboard

# ── Dashboard ───────────────────────────────────────────────
streamlit==1.35.0

# ── Jupyter environment ─────────────────────────────────────
jupyter==1.0.0
jupyterlab==4.2.1
ipykernel==6.29.4
ipywidgets==8.1.3        # Interactive widgets in notebook

# ── Utilities ───────────────────────────────────────────────
tqdm==4.66.4             # Progress bars (master loop)
python-dateutil==2.9.0
pytz==2024.1
warnings                 # stdlib — no install needed

# ── Optional: faster Parquet loading ───────────────────────
# fastparquet==2024.2.0  # Alternative to pyarrow; uncomment if preferred

# ── Development / linting (optional) ───────────────────────
# black==24.4.2
# flake8==7.0.0
# isort==5.13.2